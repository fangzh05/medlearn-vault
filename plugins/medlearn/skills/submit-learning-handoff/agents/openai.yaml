interface:
  display_name: "Submit MedLearn Handoff"
  short_description: "Submit an explicit Project Source handoff"
  default_prompt: "Read the explicitly selected MedLearnHandoff Project Source and submit it through the MedLearn tool without adding evidence."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: "mcp"
      value: "medlearn-work"
      description: "Authenticated MedLearn learning handoff submission"
      transport: "streamable_http"
      url: "https://medlearn-cloud.fzh050531.workers.dev/mcp"
