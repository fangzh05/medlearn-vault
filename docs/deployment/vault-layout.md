# Approved Vault layout

The approved publication plane uses this synchronized layout:

```text
MedLearn/
  Captures/
  Data/
    Captures/
  Reviews/
  System/
```

Required synchronized data must not use directories beginning with `.` or `_`. This layout is for
the future approved publisher; this repository does not yet write these files.
