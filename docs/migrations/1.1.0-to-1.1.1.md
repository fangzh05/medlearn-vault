# Contract migration 1.1.0 to 1.1.1

- Rename `fingerprint` to `match_fingerprint`; add full `content_hash`.
- Replace claim `course_relevance` mapping with immutable `{course_id, score}` records.
- Split claim lifecycle into `claim_status`; enforce the evidence/verification matrix.
- Replace relation citations with `supporting_claim_ids`.
- Rename misconception `error_logic` / `correct_logic` to `observed_error_logic` /
  `proposed_correction`, with optional `correction_claim_ids`.
- Make invariant-bearing collections immutable tuples.
- Add top-level versions and schemas for `ConceptRelation` and `DisciplineLens`.
