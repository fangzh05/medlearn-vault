# 0.5.0 workflow capture contracts

No persistent-domain migration is required: its contract remains 1.2.0. Package 0.5.0 adds the
independent workflow contract 0.1.0, committed workflow schemas, deterministic proposal digests,
and non-writing `capture` CLI commands. Future workflow changes require their own version and
snapshot update; they do not implicitly change persistent records.
