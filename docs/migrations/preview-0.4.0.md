# Preview 0.4.0

The package now supports topic-driven GERD and COPD previews with bilingual first-use expansion.
This adds non-persisted `PreviewRequest`, terminology, warnings, and rendering models only.

The persisted contract remains version 1.2.0, so JSON Schema snapshots are unchanged and no data
migration is required.
